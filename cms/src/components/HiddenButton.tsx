'use client'

import React from 'react'

/**
 * Renders nothing. Used to hide buttons like SaveDraft and Unpublish
 * since all saves auto-publish.
 */
export const HiddenButton: React.FC = () => null
